import "../global.css"
import { PointerLeaveProvider } from "~components/common"
import { Analytics } from "@vercel/analytics/react"

export default function App({ Component, pageProps }) {
    return (
        <>
            <PointerLeaveProvider>
                <Component {...pageProps} />
            </PointerLeaveProvider>
            <Analytics />
        </>
    )
}

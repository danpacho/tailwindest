import { Tab, Tabs, Callout } from "nextra-theme-docs"

# Easy to boot up

<Callout emoji="🚀" type="info">
    Just **install package**. That's it!
</Callout>

<Tabs items={['npm', 'yarn', 'pnpm']}>

  <Tab>
    ```bash /tailwindest/ 
    npm i tailwindest
    ```
  </Tab>
  <Tab>
    ```bash /tailwindest/ 
    yarn i tailwindest
    ```
  </Tab>
  <Tab>
    ```bash /tailwindest/ 
    pnpm i tailwindest
    ```
  </Tab>
  
</Tabs>

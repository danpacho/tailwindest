export type TailwindHardwareAccelerationType = {
    /**
     *@note If your transition performs better when rendered by the GPU instead of the CPU, you can force hardware acceleration by adding the `transform-gpu` utility
     *@docs [transform-gpu](https://tailwindcss.com/docs/translate#hardware-acceleration)
     */
    transformGPU: "transform-gpu"
}

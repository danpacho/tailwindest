"use client"

import * as React from "react"
import { type DialogProps } from "@radix-ui/react-dialog"
import { Command as CommandPrimitive } from "cmdk"
import { Search } from "lucide-react"
import { Dialog, DialogContent } from "@/registry/new-york/ui/dialog"
import { tw } from "~/tw";

const commandCommandPrimitive = tw.style({
    display: "flex",
    height: "h-full",
    width: "w-full",
    flexDirection: "flex-col",
    overflow: "overflow-hidden",
    borderRadius: "rounded-md",
    backgroundColor: "bg-popover",
    color: "text-popover-foreground",
});

const Command = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive>
>(({ className, ...props }, ref) => (
  <CommandPrimitive
    ref={ref}
    className={tw.join(commandCommandPrimitive.class(), className)}
    {...props}
  />
))
Command.displayName = CommandPrimitive.displayName


const commandDialogCommand = tw.style({
    "[&_[cmdk-group-heading]]": {
        padding: "[&_[cmdk-group-heading]]:px-2",
        fontWeight: "[&_[cmdk-group-heading]]:font-medium",
        color: "[&_[cmdk-group-heading]]:text-muted-foreground",
    },
    "[&_[cmdk-group]:not([hidden])_~[cmdk-group]]": {
        padding: "[&_[cmdk-group]:not([hidden])_~[cmdk-group]]:pt-0",
    },
    "[&_[cmdk-group]]": {
        padding: "[&_[cmdk-group]]:px-2",
    },
    "[&_[cmdk-input-wrapper]_svg]": {
        height: "[&_[cmdk-input-wrapper]_svg]:h-5",
        width: "[&_[cmdk-input-wrapper]_svg]:w-5",
    },
    "[&_[cmdk-input]]": {
        height: "[&_[cmdk-input]]:h-12",
    },
    "[&_[cmdk-item]]": {
        padding: ["[&_[cmdk-item]]:px-2", "[&_[cmdk-item]]:py-3"],
    },
    "[&_[cmdk-item]_svg]": {
        height: "[&_[cmdk-item]_svg]:h-5",
        width: "[&_[cmdk-item]_svg]:w-5",
    },
});

const CommandDialog = ({ children, ...props }: DialogProps) => {
  return (
    <Dialog {...props}>
      <DialogContent className={tw.join("overflow-hidden p-0")}>
        <Command className={commandDialogCommand.class()}>
          {children}
        </Command>
      </DialogContent>
    </Dialog>
  )
}


const commandInputCommandPrimitiveInput = tw.style({
    display: "flex",
    height: "h-10",
    width: "w-full",
    borderRadius: "rounded-md",
    backgroundColor: "bg-transparent",
    padding: "py-3",
    fontSize: "text-sm",
    outlineStyle: "outline-none",
    placeholder: {
        color: "placeholder:text-muted-foreground",
    },
    disabled: {
        cursor: "disabled:cursor-not-allowed",
        opacity: "disabled:opacity-50",
    },
});

const commandInputSearch = tw.style({
    margin: "mr-2",
    height: "h-4",
    width: "w-4",
    flexShrink: "shrink-0",
    opacity: "opacity-50",
});

const CommandInput = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.Input>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Input>
>(({ className, ...props }, ref) => (
  <div className={tw.join("flex items-center border-b px-3")} cmdk-input-wrapper="">
    <Search className={commandInputSearch.class()} />
    <CommandPrimitive.Input
      ref={ref}
      className={tw.join(commandInputCommandPrimitiveInput.class(), className)}
      {...props}
    />
  </div>
))

CommandInput.displayName = CommandPrimitive.Input.displayName

const CommandList = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.List>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.List>
>(({ className, ...props }, ref) => (
  <CommandPrimitive.List
    ref={ref}
    className={tw.join("max-h-[300px] overflow-y-auto overflow-x-hidden", className)}
    {...props}
  />
))

CommandList.displayName = CommandPrimitive.List.displayName

const CommandEmpty = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.Empty>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Empty>
>((props, ref) => (
  <CommandPrimitive.Empty
    ref={ref}
    className={tw.join("py-6 text-center text-sm")}
    {...props}
  />
))

CommandEmpty.displayName = CommandPrimitive.Empty.displayName

const CommandGroup = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.Group>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Group>
>(({ className, ...props }, ref) => (
  <CommandPrimitive.Group
    ref={ref}
    className={tw.join("overflow-hidden p-1 text-foreground [&_[cmdk-group-heading]]:px-2 [&_[cmdk-group-heading]]:py-1.5 [&_[cmdk-group-heading]]:text-xs [&_[cmdk-group-heading]]:font-medium [&_[cmdk-group-heading]]:text-muted-foreground", className)}
    {...props}
  />
))

CommandGroup.displayName = CommandPrimitive.Group.displayName

const CommandSeparator = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.Separator>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Separator>
>(({ className, ...props }, ref) => (
  <CommandPrimitive.Separator
    ref={ref}
    className={tw.join("-mx-1 h-px bg-border", className)}
    {...props}
  />
))
CommandSeparator.displayName = CommandPrimitive.Separator.displayName


const commandItemCommandPrimitiveItem = tw.style({
    position: "relative",
    display: "flex",
    cursor: "cursor-default",
    gap: "gap-2",
    userSelect: "select-none",
    alignItems: "items-center",
    borderRadius: "rounded-sm",
    padding: ["px-2", "py-1.5"],
    fontSize: "text-sm",
    outlineStyle: "outline-none",
    "data-[disabled=true]": {
        pointerEvents: "data-[disabled=true]:pointer-events-none",
        opacity: "data-[disabled=true]:opacity-50",
    },
    "data-[selected=true]": {
        backgroundColor: "data-[selected=true]:bg-accent",
        color: "data-[selected=true]:text-accent-foreground",
    },
    "[&_svg]": {
        pointerEvents: "[&_svg]:pointer-events-none",
        width: "[&_svg]:size-4",
        flexShrink: "[&_svg]:shrink-0",
    },
});

const CommandItem = React.forwardRef<
  React.ElementRef<typeof CommandPrimitive.Item>,
  React.ComponentPropsWithoutRef<typeof CommandPrimitive.Item>
>(({ className, ...props }, ref) => (
  <CommandPrimitive.Item
    ref={ref}
    className={tw.join(commandItemCommandPrimitiveItem.class(), className)}
    {...props}
  />
))

CommandItem.displayName = CommandPrimitive.Item.displayName

const CommandShortcut = ({
  className,
  ...props
}: React.HTMLAttributes<HTMLSpanElement>) => {
  return (
    <span
      className={tw.join("ml-auto text-xs tracking-widest text-muted-foreground", className)}
      {...props}
    />
  )
}
CommandShortcut.displayName = "CommandShortcut"

export {
  Command,
  CommandDialog,
  CommandInput,
  CommandList,
  CommandEmpty,
  CommandGroup,
  CommandItem,
  CommandShortcut,
  CommandSeparator,
}

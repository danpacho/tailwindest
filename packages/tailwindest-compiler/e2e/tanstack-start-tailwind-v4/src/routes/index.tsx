import { createFileRoute } from "@tanstack/react-router"
import { TailwindestParityFixture } from "../tailwindest-parity-fixture"

export const Route = createFileRoute("/")({
    component: TailwindestParityFixture,
})
